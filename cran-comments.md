## Resubmission
This is a patch release. 
`get_diffNetworks_singleOmic()` now double checks that `assayData` and 
`metadata` are aligned for the layer.

## R CMD check results
0 errors ✔ | 0 warnings ✔ | 0 note ✔
